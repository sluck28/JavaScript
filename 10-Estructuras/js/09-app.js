//operador ternario

const autenticado=true;
const puedePagar =false;
//podemos utilizar && || en el operador ternario

console.log(autenticado && puedePagar ?  'esta autenticado' : ' no esta autenticado');